import { css } from "@emotion/react";

export const styles = {
  movieFilters: css({
    display: "flex",
    justifyContent: "center",
    marginTop: 20,
  }),
  filter: css({
    backgroundColor: "#ffffff",
    borderRadius: '4px',
  }),
};
